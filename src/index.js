import './main.scss'


class Form {
	constructor() {
		let number = [5,10,20].map(number => number * 2)
		console.log("This is the constructor of the Form Class we don't need bable for it.")
		console.log(number)
	}
}

new Form()